# Minimal runtime dependencies for the Hydra-first phase of the starter project.
# Prefer `pip install -e .` or `pip install -e ".[dev]"` when working inside the repo.
hydra-core>=1.3,<2.0
omegaconf>=2.3,<3.0
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0
