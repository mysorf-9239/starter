defaults:
  - _self_
  - env: local
  - paths: default
  - runtime: default
  - logging: console
  - tracking: disabled
  - profiling: basic
  - artifacts: default
  - sweeps: default
  - hydra: default

app:
  name: starter
  subsystem: config
  version: 0.1.0
